
var App = {
    /* 
     * init
     * Initialize the app
     */
    init: function() {
        /* Add new objects to window */
        Book.init();
        ChristmasTree.init();
        AmbientLightManager.init();
    }
};